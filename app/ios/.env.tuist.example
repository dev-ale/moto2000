# Copy to `.env.tuist` (gitignored) and fill in your Apple Developer Team ID.
# Tuist reads any `TUIST_*` var and exposes it as `Environment.camelCased` in Project.swift.
TUIST_DEVELOPMENT_TEAM=ABCD123456
